---
globs: '["scripts/python/**"]'
description: This rule ensures consistent organization of Python projects in a
  dedicated scripts/python directory structure
---

For new Python projects, create them in scripts/python directory where the directories are named after each script.